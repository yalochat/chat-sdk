# Copyright (c) Yalochat, Inc. All rights reserved.

name: Android SDK Pipeline

on:
  push:
    branches:
      - 'main'
    paths:
      - 'android-sdk/**'
      - '.github/workflows/android-sdk-ci.yml'

  pull_request:
    paths:
      - 'android-sdk/**'
      - '.github/workflows/android-sdk-ci.yml'

jobs:
  quality-gate:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./android-sdk

    steps:
      - uses: actions/checkout@v5

      - name: Gitleaks Scan
        run: docker run --rm -v ${{ github.workspace }}:/code zricethezav/gitleaks:latest detect --source="/code" -v

      - name: Setup Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Cache Gradle
        uses: actions/cache@v5
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('android-sdk/gradle/libs.versions.toml') }}
          restore-keys: ${{ runner.os }}-gradle-

      - name: Lint
        run: ./gradlew :sdk:lint

      - name: Unit tests
        run: ./gradlew :sdk:testDebugUnitTest

      - name: Build release AAR
        run: ./gradlew :sdk:assembleRelease
